<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feliz Día de San Valentín</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to bottom, #FFD1DC, #FFB6C1);
            overflow-x: hidden;
        }

        header {
            text-align: center;
            background-color: #FF6F91;
            color: white;
            padding: 30px 20px;
        }

        header h1 {
            font-size: 3em;
            margin: 0;
        }

        main {
            padding: 40px 20px;
            text-align: center;
        }

        .messages {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 40px;
        }

        .message-card {
            background-color: white;
            border-radius: 15px;
            padding: 20px;
            width: 200px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            transition: transform 0.3s, box-shadow 0.3s;
            cursor: pointer;
        }

        .message-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
        }

        .heart {
            color: red;
            font-size: 2em;
            animation: beat 1s infinite;
        }

        @keyframes beat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.2); }
        }

        form {
            background-color: white;
            padding: 30px;
            max-width: 400px;
            margin: 0 auto 50px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        form input, form textarea {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 10px;
            border: 1px solid #ccc;
            font-size: 1em;
        }

        form button {
            background-color: #FF6F91;
            color: white;
            padding: 15px 20px;
            border: none;
            border-radius: 10px;
            font-size: 1em;
            cursor: pointer;
            transition: background 0.3s;
        }

        form button:hover {
            background-color: #FF4C6A;
        }

        footer {
            text-align: center;
            padding: 20px;
            background-color: #FF6F91;
            color: white;
        }

        /* Floating hearts animation */
        .floating-heart {
            position: fixed;
            font-size: 24px;
            color: red;
            animation: float 7s linear infinite;
            pointer-events: none;
        }

        @keyframes float {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-10vh) rotate(360deg); opacity: 0; }
        }
    </style>
</head>
<body>

    <header>
        <h1>¡Feliz Día de San Valentín! 💖</h1>
    </header>

    <main>
        <h2>Mensajes para tus seres queridos</h2>
        <div class="messages">
            <div class="message-card">Eres mi persona favorita ❤️</div>
            <div class="message-card">Contigo, cada día es especial 💕</div>
            <div class="message-card">Te quiero hasta la luna y de regreso 🌙</div>
            <div class="message-card">Gracias por hacerme sonreír siempre 😘</div>
        </div>

        <h2>Envía tu mensaje personalizado</h2>
        <form id="valentineForm">
            <input type="text" id="name" placeholder="Tu nombre" required>
            <input type="text" id="recipient" placeholder="Nombre del destinatario" required>
            <textarea id="message" rows="4" placeholder="Tu mensaje de amor..." required></textarea>
            <button type="submit">Enviar 💌</button>
        </form>
    </main>

    <footer>
        <p>© 2026 Día de San Valentín - Amor para todos</p>
    </footer>

    <script>
        // Form submit action
        const form = document.getElementById('valentineForm');
        form.addEventListener('submit', function(e) {
            e.preventDefault();
            const name = document.getElementById('name').value;
            const recipient = document.getElementById('recipient').value;
            const message = document.getElementById('message').value;
            alert(`¡Mensaje enviado!\nDe: ${name}\nPara: ${recipient}\nMensaje: ${message}`);
            form.reset();
        });

        // Floating hearts effect
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('floating-heart');
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.fontSize = (10 + Math.random() * 30) + 'px';
            heart.textContent = '❤️';
            document.body.appendChild(heart);

            setTimeout(() => {
                heart.remove();
            }, 7000);
        }

        setInterval(createHeart, 500); // create a heart every 0.5 seconds
    </script>
</body>
</html>
